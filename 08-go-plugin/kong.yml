_format_version: "3.0"

services:
  - name: test-service
    url: http://httpbin:80
    routes:
      - name: test-route
        paths:
          - /api
        methods:
          - GET
          - POST

plugins:
  - name: simple-go-plugin
    service: test-service
